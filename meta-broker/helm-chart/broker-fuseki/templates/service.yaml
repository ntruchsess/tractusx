apiVersion: v1
kind: Service
metadata:
  name: {{ include "broker-fuseki.fullname" . }}
  labels:
    {{- include "broker-fuseki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "broker-fuseki.selectorLabels" . | nindent 4 }}
